# CVNP1601 Linux Administration Portfolio

Coursework and evidence for CVNP1601. One folder per week.

## What I Can Do
Mapped a Linux lab VM over SSH, filtered /var/log/syslog with grep and safe redirection, and documented a break-fix for an overwritten evidence file.

Edited a server note in vim, wrote a short nano note, pulled active SSH settings with grep -v, and built an accepted-login access report with awk. Diagnosed an unsorted uniq IP count before it could reach security.

## AI use
I used AI when I got stuck on setup and writeup structure. I ran every lab command on my VM and the writeups match what I actually did.
